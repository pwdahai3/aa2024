# vue


#vuejs angularreact
#中文注释
> A Vue.js project

## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm run dev

# build for production with minification
npm run build

# build for production and view the bundle analyzer report
npm run build --report
```
最新版本，去了正则 2022-05-05
加网卡名
config.js域名base64编码

加换logo背景功能

改shadow.access.domain===>shadow.access.http_host
匹配方式多加 自定义配置 customMatch
wafui